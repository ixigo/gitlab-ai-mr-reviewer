# GitLab Configuration
CI_SERVER_URL=https://www.gitlab.com
CI_PROJECT_ID=your-project-id
PROJECT_ID=your-project-id
CI_MERGE_REQUEST_IID=123
GITLAB_TOKEN=your-gitlab-token
CI_JOB_TOKEN=your-ci-job-token

# Cursor API Configuration
CURSOR_API_KEY=your-cursor-api-key

# Tech stack is now AUTO-DETECTED - no configuration needed!
#   - 'next-ts': Auto-detected if package.json has 'next' and 'react' dependencies
#   - 'java-maven': Auto-detected if pom.xml exists with Java source files
#   - 'java-gradle': Auto-detected if build.gradle exists with Java source files
#   - 'kotlin': Auto-detected if build.gradle.kts exists with Kotlin source files
# 
# The REVIEW_CONFIG_TYPE environment variable is no longer supported

# Review Type: Always reviews the complete MR diff
# This provides comprehensive context for every review

# Optional: Override default paths
# All review output files (diff, markdown, JSON, changed files) go here
OUTPUT_DIR=.cursor/reviews

# Grading Configuration (optional - defaults in src/constants/grading.ts)
# GRADING_MIN_SCORE=70           # Minimum score to pass (0-100)
# GRADING_MIN_GRADE=C            # Minimum grade to pass (F, D-, D, D+, C-, C, C+, B-, B, B+, A, A+)
# GRADING_MAX_CRITICAL=3         # Maximum critical issues allowed
# GRADING_MAX_MODERATE=8         # Maximum moderate issues allowed

# Quality Gate Enforcement (optional - defaults to true)
# MUST_PASS_REVIEW_QUALITY=true  # If true, exit with code 1 when quality thresholds are not met (fails CI pipeline)
#                                 # If false, log warnings but exit with code 0 (allows pipeline to pass)
#                                 # Default: true (strict enforcement enabled)

